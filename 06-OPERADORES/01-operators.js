'Use Strict';

let a = Number (prompt ("Ingrese-Valor-a")); //declarando la variable y asignando 1
let b = Number (prompt ("Ingrese-Valor-b")); //declarando
let result;
// Almacenar en la variable result la suma de a y b
// Luego mostrar el contenido en result
result = a+b;

//alert (result);

result = a*b;

//alert (result);

result = a%b;

//alert (result); 
// Crear una variable a2 y asignar el valor de a en negativo.
// Mostrar el resultado de a2
let a2 = -a;
a++; // Incrementa el valor de a en 1
alert (a);

b--; // Decrementa el valor de b en 1
alert (b);  