'Use Strict';

//Forma más común
function sum (a,b) {
    return a+b;
}

//Expresion de función
let sumExpresion = function (a,b) {
    return a+b;
}

//Funcion Flecha
let sumFlecha = (a,b) => a + b;

//Llamar a las funciones
let result = sum (2,3);
alert(result);
